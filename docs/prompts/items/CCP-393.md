Prompt ID: CCP-393
Task ID: CCP-393
Source Document: docs/mini-sprints/IMPROVEMENT_INTELLIGENCE_PLATFORM_SPRINT_2026-03-29.md
Source Step: Task 15 — Conversion and resourcefulness metrics
Execution Target: Claude Code

You are working in `/Users/leftcoast/Development/PatzerPatzer`.

Queue execution marker step:
- As the first task before startup coordination or implementation work, run:
  - `npm run prompt:start -- CCP-393`
- Only continue implementation work after that command succeeds.
- Leave this prompt queued after marking it started, even if execution later fails, stops midway, or hits a blocker.

Startup coordination step:
- Before editing, check whether any other tool, agent, Claude Code session, or Codex thread is actively touching the same stats view / conversion metrics / prompt-tracking files.
- If overlapping work exists, stop and report it before editing.

Task: add conversion and resourcefulness metrics to the stats page, including sample-size gating and optional links back to failed-conversion games where appropriate.

Inspect first:
- `src/stats/view.ts`
- `src/stats/types.ts`
- `src/stats/extract.ts`
- `docs/mini-sprints/IMPROVEMENT_INTELLIGENCE_PLATFORM_SPRINT_2026-03-29.md`

Constraints:
- require at least 5 qualifying games for each metric
- hide metrics below threshold instead of showing misleading values
- keep any game-link list small and high-signal

Before coding, provide:
- prompt id
- task id
- source document
- source step
- task title
- relevant Patzer Pro files
- diagnosis
- exact small step to implement
- why that step is safely scoped

Then implement the change directly.

Validation is required after coding:
- run `npm run build`
- report whether conversion/resourcefulness metrics render only when valid
- report whether failed-conversion links work if implemented
- report console/runtime errors
- report remaining risks and limitations

Also include a short manual test checklist with concrete user actions and expected results.
