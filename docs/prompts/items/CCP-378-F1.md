Read and follow:
- `/Users/leftcoast/Development/PatzerPatzer/CLAUDE.md`
- `/Users/leftcoast/Development/PatzerPatzer/AGENTS.md`

Prompt ID: CCP-378-F1
Task ID: CCP-378
Parent Prompt ID: CCP-378
Source Document: CCP-378 review
Source Step: Fix practice-mode movable-color refresh and render-time opponent planning
Execution Target: Claude Code

## Lifecycle

Before making any changes, mark this prompt as started:
```sh
npm run prompt:start -- CCP-378-F1
```

After all work is complete, mark it as done:
```sh
npm run prompt:complete -- CCP-378-F1 --checklist "- [ ] Start Practice Against Them from a position that is unchanged from browse mode — expected result: opponent pieces become non-draggable immediately when practice starts|- [ ] Stop practice without changing the board position — expected result: browse mode restores normal movable behavior immediately|- [ ] Play moves until the opponent source banner is shown — expected result: the banner stays accurate without doing random move-selection work inside render|- [ ] Open DevTools during practice start/stop and normal redraws — expected result: no runtime or console errors"
```

If errors or issues were encountered during execution, use `--errors` instead:
```sh
npm run prompt:complete -- CCP-378-F1 --errors "brief description of what went wrong" --checklist "- [ ] Start Practice Against Them from a position that is unchanged from browse mode — expected result: opponent pieces become non-draggable immediately when practice starts|- [ ] Stop practice without changing the board position — expected result: browse mode restores normal movable behavior immediately|- [ ] Play moves until the opponent source banner is shown — expected result: the banner stays accurate without doing random move-selection work inside render|- [ ] Open DevTools during practice start/stop and normal redraws — expected result: no runtime or console errors"
```

You are working in `/Users/leftcoast/Development/PatzerPatzer`.

Startup coordination step:
- Before editing, check whether any other tool, agent, Claude Code session, or Codex thread is actively touching the same openings practice, board, engine, shell, or prompt-tracking files.
- If overlapping work exists, stop and report it before editing.

Task: fix the two remaining Practice Against Them issues from `CCP-378`: stale `movable.color` when practice mode changes at the same FEN, and render-time use of `planOpponentTurn()`.

Review-grounded issues to confirm:
- `CCP-378` remains `issues found`
- in `src/openings/view.ts`, `syncOpeningsBoard()` still returns early when FEN is unchanged, so practice start/stop can leave board interactivity stale if the position itself did not change
- `renderPracticeActivePanel()` still calls `planOpponentTurn()` during render even though that function performs opponent-selection work and may use randomness

Inspect first:
- Patzer:
  - `src/openings/view.ts`
  - `src/openings/practice.ts`
  - `src/engine/playMove.ts`
  - `docs/prompts/items/CCP-378.md`
- Relevant Lichess reference:
  - inspect the closest Lichess board-led training ownership pattern if needed, but this is primarily a Patzer state/render correctness fix

Before coding, provide:
- prompt id
- task id
- parent prompt id
- source document
- source step
- task title
- relevant Patzer Pro files
- relevant Lichess files
- exact diagnosis
- exact small step to implement
- why that step is safe and correctly scoped

Implementation goal:
- practice start/stop must always update board interactivity correctly, even at the same position
- opponent-turn planning should not happen inside render-time banner composition
- preserve existing practice behavior otherwise

Constraints:
- do not redesign Practice Against Them
- do not broaden this into another practice-mode feature pass
- keep the fix scoped to board sync correctness and render/ownership cleanup
- prefer the smallest safe structural change that removes render-time planning work

Then implement the change directly.

Validation is required after coding:
- run `npm run build`
- run `npx tsc --noEmit`
- run the most relevant task-specific check you can for practice mode start/stop and opponent response behavior
- explicitly report:
  - what changed
  - whether behavior changed intentionally
  - whether practice start/stop now refreshes interactivity correctly
  - whether render-time opponent planning was removed
  - whether there are console/runtime errors
  - remaining risks and limitations

Suggested manual verification focus:
- start practice from the current opening-tree position without changing FEN first
- stop practice immediately at the same FEN
- verify opponent source banner remains accurate through repertoire and engine phases
