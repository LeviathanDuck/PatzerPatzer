Prompt ID: CCP-548
Task ID: CCP-548
Source Document: docs/mini-sprints/STUDY_PAGE_SPRINT_2026-03-31.md
Source Step: Phase 4 — Repetition Practice — Core Engine
Execution Target: Claude Code

You are working in `/Users/leftcoast/Development/PatzerPatzer`.

Read and follow:
- `/Users/leftcoast/Development/PatzerPatzer/AGENTS.md`
- `/Users/leftcoast/Development/PatzerPatzer/CLAUDE.md`
- `/Users/leftcoast/Development/PatzerPatzer/docs/prompts/README.md`
- `/Users/leftcoast/Development/PatzerPatzer/docs/prompts/CODEX_PROMPT_INSTRUCTIONS.md`

Sprint reference:
- `/Users/leftcoast/Development/PatzerPatzer/docs/mini-sprints/STUDY_PAGE_SPRINT_2026-03-31.md`

Batch prompt IDs to execute in order:
- `CCP-543`
- `CCP-544`
- `CCP-545`
- `CCP-546`
- `CCP-547`

Manager-prompt rule:
- `CCP-548` is the manager prompt id only
- do not execute or recurse into `CCP-548` as if it were one of the child prompts

Phase goal: Pure logic layer: scheduling, grading, drill session control. No UI — testable TypeScript modules only.

Startup coordination step:
- Before editing, check whether any other tool, agent, Claude Code session, or Codex thread is actively touching the same repo areas.
- If overlapping work exists, stop and report it before editing.

Task:
- read the queued child prompts exactly as written from the items directory
- execute them sequentially in the exact order listed above
- before each child prompt, run `npm run prompt:start -- <CHILD_PROMPT_ID>`
- perform internal validation and self-check after each prompt
- stop immediately on any real issue, failed validation, or unsafe repo state

Execution rules:
- do not reorder child prompts
- do not create new prompts during the batch
- do not continue past a known issue just to finish the phase
- use internal validation/self-check only

After each completed child prompt, report:
- Prompt ID, task title, build result, typecheck result, validation result, continue/stop

Phase validation — after all child prompts complete:
After Phase 4 completes, verify:
- `npx tsc --noEmit` passes for all new modules
- scheduler: scheduleNext(3, true) → {newLevel:4, nextDueAt: ~2weeks from now}
- scheduler: scheduleNext(5, false) → {newLevel:3, nextDueAt: ~1week from now}
- scheduler: scheduleNext(1, false) → {newLevel:1, nextDueAt: ~1day} (min level 1)
- grader: gradeMove("Nf3", "Nf3") → "correct"
- grader: gradeMove("e4", "Nf3") → "incorrect"
- drillCtrl: correct move → feedback "correct"
- drillCtrl: wrong 3 times → feedback "showAnswer"
- sessionBuilder: given due positions → sorted sequences
- extractLine: PGN → TrainableSequence with correct moves/sans/fens
- All modules pure (no IDB, no board, no DOM)
