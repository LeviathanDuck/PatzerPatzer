Read and follow:
- `/Users/leftcoast/Development/PatzerPatzer/CLAUDE.md`
- `/Users/leftcoast/Development/PatzerPatzer/AGENTS.md`

Prompt ID: CCP-370-F1
Task ID: CCP-370
Parent Prompt ID: CCP-370
Source Document: CCP-370 review
Source Step: Fix duplicated top3EcoPct signal in Style dashboard
Execution Target: Claude Code

## Lifecycle

Before making any changes, mark this prompt as started:
```sh
npm run prompt:start -- CCP-370-F1
```

After all work is complete, mark it as done:
```sh
npm run prompt:complete -- CCP-370-F1 --checklist "- [ ] Open the Style tool on a collection with enough games to show all sections — expected result: the dashboard still shows the full style surface without console errors|- [ ] Inspect the style axes and behavioral tendencies sections — expected result: the top-3 opening concentration signal appears only once, not twice with near-duplicate wording|- [ ] Switch between collections with different repertoire concentration — expected result: the remaining style signal updates coherently and does not leave stale duplicated copy"
```

If errors or issues were encountered during execution, use `--errors` instead:
```sh
npm run prompt:complete -- CCP-370-F1 --errors "brief description of what went wrong" --checklist "- [ ] Open the Style tool on a collection with enough games to show all sections — expected result: the dashboard still shows the full style surface without console errors|- [ ] Inspect the style axes and behavioral tendencies sections — expected result: the top-3 opening concentration signal appears only once, not twice with near-duplicate wording|- [ ] Switch between collections with different repertoire concentration — expected result: the remaining style signal updates coherently and does not leave stale duplicated copy"
```

You are working in `/Users/leftcoast/Development/PatzerPatzer`.

Startup coordination step:
- Before editing, check whether any other tool, agent, Claude Code session, or Codex thread is actively touching the same openings style, analytics, styling, or prompt-tracking files.
- If overlapping work exists, stop and report it before editing.

Task: fix the Style dashboard so it no longer renders the same `top3EcoPct` concentration metric twice in different sections.

Review-grounded issue to confirm:
- `CCP-370` remains `issues found`
- in `src/openings/view.ts`, `renderStyleAxesBars()` still renders the top-3 opening concentration as the `Opening variety` axis
- `renderStyleBehavioral()` still renders the same `top3EcoPct` metric again as `opening commitment`
- this produces duplicate user-facing meaning with slightly different wording inside the same dashboard

Inspect first:
- Patzer:
  - `src/openings/view.ts`
  - `src/openings/analytics.ts`
  - `docs/prompts/items/CCP-370.md`
- Relevant Lichess reference:
  - inspect only if needed to confirm comparable dashboard-density or anti-duplication patterns; this is primarily a Patzer presentation/interpretation cleanup

Before coding, provide:
- prompt id
- task id
- parent prompt id
- source document
- source step
- task title
- relevant Patzer Pro files
- relevant Lichess files
- exact diagnosis
- exact small step to implement
- why that step is safe and correctly scoped

Implementation goal:
- keep the Style dashboard useful and readable
- remove the duplicated top-3 opening concentration signal
- preserve the rest of the dashboard structure unless a tiny wording adjustment is needed

Constraints:
- do not redesign the whole Style tool
- do not rewrite the analytics model unless the current duplication truly comes from the model boundary
- prefer the smallest safe change in presentation/composition
- do not bundle unrelated style-dashboard cleanup

Then implement the change directly.

Validation is required after coding:
- run `npm run build`
- run `npx tsc --noEmit`
- run the most relevant task-specific check you can for the Style dashboard rendering
- explicitly report:
  - what changed
  - whether behavior changed intentionally
  - whether the duplicated signal is gone
  - whether there are console/runtime errors
  - remaining risks and limitations

Suggested manual verification focus:
- collections with enough games to show both Style axes and Behavioral tendencies
- collections with concentrated vs varied repertoires
