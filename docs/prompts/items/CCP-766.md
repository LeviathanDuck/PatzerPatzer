Read and follow:
- `/Users/leftcoast/Development/PatzerPatzer/CLAUDE.md`
- `/Users/leftcoast/Development/PatzerPatzer/AGENTS.md`

# CCP-766 — Review LFYM Hotfix Cluster

## Summary

Review the LFYM hotfix cluster prompts in exact order. This manager is review-only:
- do not execute implementation work again
- do not create follow-up prompts unless a child review proves one is needed under the review workflow
- review child prompts in the exact order listed below
- close out each child prompt first
- only after all child reviews are complete, review and close this manager

## Child Prompt Order

1. `CCP-682`
2. `CCP-683`
3. `CCP-685`
4. `CCP-680-F1`
5. `CCP-680-F2`
6. `CCP-680-F3`
7. `CCP-680-F4`
8. `CCP-680-F5`
9. `CCP-680-F6`
10. `CCP-680-F7`
11. `CCP-680-F8`
12. `CCP-687`
13. `CCP-688`
14. `CCP-689`

Before reviewing each child:
- read the child prompt body fully
- inspect the current code or docs the prompt changed
- compare the implementation against the prompt contract and current repo reality
- record findings before summary if the prompt is still wrong
- use `npm run prompt:review -- ... --reviewed-by Codex --review-method ...` with the method that fits the child prompt type

## Execution Rules

- review the children in exact order
- do not reorder or skip ahead
- do not mark this manager reviewed while any child above remains unreviewed
- if a child still has live issues, record them honestly and keep the manager blocked until child reviews are complete
- if a child review requires a follow-up fix prompt, create it only after the review establishes the need

## Final Report

Report:
- manager prompt id
- ordered child prompt list
- result for each child:
  - completed cleanly
  - completed with errors
  - not run
- exact stopping point if review halted early
- any unresolved issues or follow-up prompts created

## Validation

- every child prompt listed above is no longer in `queued-run` / unreviewed state after the batch review
- every child review uses explicit provenance fields:
  - `reviewedBy`
  - `reviewMethod`
- this manager is reviewed only after all child prompts are reviewed
- `npm run prompts:refresh` completes after the batch review

## Lifecycle

Before reviewing any child prompts, mark this manager as started:
```sh
npm run prompt:start -- CCP-766
```

After all child prompts are reviewed, mark the manager as done:
```sh
npm run prompt:complete -- CCP-766 --checklist "- [ ] All 14 child prompts were reviewed in order|- [ ] Every child review recorded reviewedBy and reviewMethod|- [ ] CCP-766 was reviewed only after all child prompts were reviewed|- [ ] npm run prompts:refresh completed after the batch review"
```

If errors or issues were encountered during execution, use `--errors` instead:
```sh
npm run prompt:complete -- CCP-766 --errors "One or more child reviews could not be completed or the manager could not be honestly closed" --checklist "- [ ] All reachable child prompts were reviewed in order|- [ ] Any blocked child review was recorded honestly|- [ ] CCP-766 remained open or issues-found if child review state was incomplete|- [ ] npm run prompts:refresh completed after the partial batch review"
```
