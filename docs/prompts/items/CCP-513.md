Prompt ID: CCP-513
Task ID: CCP-513
Source Document: user request
Source Step: Style color toggle buttons with username display
Execution Target: Claude Code

You are working in `/Users/leftcoast/Development/PatzerPatzer`.

Queue execution marker step:
- As the first task before startup coordination or implementation work, run:
  - `npm run prompt:start -- CCP-513`
- Only continue implementation work after that command succeeds.

Task: On the opening tree page, update the White/Black color toggle buttons so that when a button is active (selected), the current import username appears as subtle smaller text on the top line, with the color label (WHITE / BLACK) below it. Both buttons must always be the same height regardless of active state — the username line takes space even when empty. The username appears on the active button only and swaps when the other button is pressed.

Files to touch:
- `src/openings/view.ts` — restructure button children to a two-line layout
- `src/styles/main.scss` — update button flex direction, add username + label element styles

Constraints:
- keep this scoped to the two files above
- do not change button active colors, hover states, or other non-layout styles
- do not bundle unrelated cleanup
