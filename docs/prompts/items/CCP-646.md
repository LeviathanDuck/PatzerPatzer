# CCP-646 — Sprint dashboard UI polish batch manager

Read and follow:
- `/Users/leftcoast/Development/PatzerPatzer/CLAUDE.md`
- `/Users/leftcoast/Development/PatzerPatzer/AGENTS.md`

Prompt ID: CCP-646
Task ID: CCP-646
Source Document: ad hoc user request
Source Step: Have Claude review the sprint tab in the dashboard and make design improvements
Execution Target: Claude Code

## Batch prompt IDs to execute in order

- `CCP-643`
- `CCP-642`
- `CCP-644`
- `CCP-645`
- `CCP-647`

## Manager rules

- `CCP-646` is the manager prompt id only
- do not recurse into `CCP-646` as if it were one of the child prompts
- execute child prompts sequentially in the exact order listed above
- do not create new prompts during execution unless a real blocker makes that unavoidable
- stop the batch on a real validation failure, unsafe overlap, or design issue that makes the next prompt unsafe

## Scope

This batch exists to improve the visual design of the sprint tab in the shared tracking dashboard.

It is intentionally split into:
- a first design audit / foundational cleanup
- card layout polish
- progress/status polish
- detail panel hierarchy polish
- responsive/accessibility cleanup

## Validation

After each child prompt:
- regenerate the dashboard if needed
- confirm the `Prompts` tab still renders correctly
- confirm the `Sprints` tab still renders correctly
- report whether the batch can continue safely

## Lifecycle

Before executing any child prompts, mark this manager as started:
```sh
npm run prompt:start -- CCP-646
```

After all child prompts are complete, mark the manager as done:
```sh
npm run prompt:complete -- CCP-646 --checklist "- [ ] All child prompts completed in the listed order|- [ ] The Sprints tab reflects the intended UI polish improvements|- [ ] The Prompts tab still renders correctly after all sprint-tab changes|- [ ] No child prompt was skipped or silently reordered"
```

If errors or issues were encountered during execution, use `--errors` instead:
```sh
npm run prompt:complete -- CCP-646 --errors "brief description of what went wrong" --checklist "- [ ] All child prompts completed in the listed order|- [ ] The Sprints tab reflects the intended UI polish improvements|- [ ] The Prompts tab still renders correctly after all sprint-tab changes|- [ ] No child prompt was skipped or silently reordered"
```
