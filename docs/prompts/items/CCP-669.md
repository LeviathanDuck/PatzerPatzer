Read and follow:
- `/Users/leftcoast/Development/PatzerPatzer/CLAUDE.md`
- `/Users/leftcoast/Development/PatzerPatzer/AGENTS.md`

# CCP-669 — Temporary Skip Validation Prompt

Validation-only prompt created to verify the skipped lifecycle path.

## Lifecycle

Before making any changes, mark this prompt as started:
```sh
npm run prompt:start -- CCP-669
```

After all work is complete, mark it as done:
```sh
npm run prompt:complete -- CCP-669 --checklist "- [ ] Validation-only prompt was exercised"
```

If errors or issues were encountered during execution, use `--errors` instead:
```sh
npm run prompt:complete -- CCP-669 --errors "validation prompt intentionally not run" --checklist "- [ ] Validation-only prompt was exercised"
```
