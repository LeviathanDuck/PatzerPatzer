# CCP-653 — Sprint detail action panels UI batch manager

Read and follow:
- `/Users/leftcoast/Development/PatzerPatzer/CLAUDE.md`
- `/Users/leftcoast/Development/PatzerPatzer/AGENTS.md`

Prompt ID: CCP-653
Task ID: CCP-653
Source Document: ad hoc user request
Source Step: Codex owns sprint backend semantics; Claude should polish the sprint detail action surfaces
Execution Target: Claude Code

## Batch prompt IDs to execute in order

- `CCP-652`
- `CCP-648`
- `CCP-649`
- `CCP-650`
- `CCP-651`

## Manager rules

- `CCP-653` is the manager prompt id only
- do not recurse into `CCP-653` as if it were one of the child prompts
- execute child prompts sequentially in the exact order listed above
- do not change sprint registry semantics or workflow rules in this batch

## Scope

This batch is only for sprint-detail presentation and interaction polish on top of the backend
contract Codex already implemented.

Claude must not:
- redefine sprint states
- redefine next available phase logic
- redefine audit truth rules
- redefine sprint prompt linkage rules

Claude should only improve:
- layout
- hierarchy
- copy surfaces
- warning presentation
- responsiveness/accessibility

## Validation

After each child prompt:
- regenerate the dashboard
- verify prompt detail still works
- verify sprint detail still works
- verify the action panels still respect the backend visibility rules

## Lifecycle

Before executing any child prompts, mark this manager as started:
```sh
npm run prompt:start -- CCP-653
```

After all child prompts are complete, mark the manager as done:
```sh
npm run prompt:complete -- CCP-653 --checklist "- [ ] All child prompts completed in order|- [ ] Sprint detail action surfaces are visually improved without changing sprint semantics|- [ ] Prompt detail and sprint detail both still work after the UI batch|- [ ] Backend visibility and state rules were preserved throughout the batch"
```

If errors or issues were encountered during execution, use `--errors` instead:
```sh
npm run prompt:complete -- CCP-653 --errors "brief description of what went wrong" --checklist "- [ ] All child prompts completed in order|- [ ] Sprint detail action surfaces are visually improved without changing sprint semantics|- [ ] Prompt detail and sprint detail both still work after the UI batch|- [ ] Backend visibility and state rules were preserved throughout the batch"
```
