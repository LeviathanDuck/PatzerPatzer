---
deployment:
  tasks:
    - /bin/bash -c 'DEPLOYPATH=/home1/leftcoc0/public_html/patzerpro && [ "$DEPLOYPATH" = "/home1/leftcoc0/public_html/patzerpro" ] || { echo "ABORT: deploy path mismatch"; exit 1; } && [ -d "$PWD/public" ] || { echo "ABORT: source directory public/ not found in $PWD"; exit 1; } && mkdir -p "$DEPLOYPATH" && rsync -a --delete --exclude=".git" "$PWD/public/" "$DEPLOYPATH/"'
