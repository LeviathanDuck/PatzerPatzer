---
deployment:
  tasks:
    - /bin/bash -c '
        DEPLOYPATH=/home1/leftcoc0/public_html/patzerpro &&
        echo "$DEPLOYPATH" | grep -q "patzerpro" || { echo "ABORT: path does not contain patzerpro"; exit 1; } &&
        [ -d "$DEPLOYPATH" ] || { echo "ABORT: target directory does not exist — refusing to deploy"; exit 1; } &&
        rsync -a --delete
          --exclude=".git"
          /home1/leftcoc0/repositories/patzerpatzer/public/
          "$DEPLOYPATH/"
      '
